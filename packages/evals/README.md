# @akcp/evals

This package contains evaluation scripts for AKCP agents.

## Usage

While tests can be run via standard `pnpm test` (which may be a no-op if no automated tests are defined), the actual evaluations can be run via:

```bash
pnpm run evals
```

This will execute `node dist/index.js` to run the evaluation scenarios.
