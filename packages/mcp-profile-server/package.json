{
  "name": "@ocf/mcp-profile-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP Profile Server manages the candidates OKF bundle files query and lifecycle validations.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.1",
    "zod": "catalog:",
    "@ocf/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "tsx": "^4.19.0"
  }
}
